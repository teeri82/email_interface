make mvp that can be run in CLI without encryption
there is a config file for needed email credentials and ID (no need for self ID feature)
multiple MVP:s can be run on same server but in different directories

first start the application and when you run the send command you put the payload message as an attribute of the command and there is a log file where the communication is written

also make app.log that will be for appfunctionalities logging